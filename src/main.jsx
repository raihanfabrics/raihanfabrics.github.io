import React from 'react'
import ReactDOM from 'react-dom/client'
import TextileApp from './TextileApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TextileApp />
  </React.StrictMode>,
)
